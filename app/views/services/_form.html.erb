<%= form_for(service) do |f| %>
  <% if service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :publishing %>
    <%= f.text_field :publishing %>
  </div>

  <div class="field">
    <%= f.label :software_products %>
    <%= f.text_field :software_products %>
  </div>

  <div class="field">
    <%= f.label :training %>
    <%= f.text_field :training %>
  </div>

  <div class="field">
    <%= f.label :clients %>
    <%= f.text_field :clients %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
